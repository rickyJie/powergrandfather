# <项目名> 自动化流程说明

> 这份文件是给 CSM 自动化模块（以及未来接手的 LLM agent）看的。
> 写清楚"这个项目自动化跑的时候，agent 应该做什么、按什么顺序、出什么"。

## 一、流程做什么

<一句话说明这个流程的目标。例如：
"每天扫一遍上游数据集，跑一遍 face recognition 评估脚本，把准确率写进 report.json。">

## 二、流程步骤

按 agent 视角写，不要写"用户先打开 X"。每步要可执行、不模糊。

1. **<步骤名>**：<这步做什么 / 跑什么命令 / 检查什么>
2. **<步骤名>**：...
3. **<步骤名>**：...

## 三、外部依赖

跑这个流程需要什么前提？例如：
- conda env：`<env_name>`
- 必须能访问的服务：`<service_url>`
- 必须存在的配置文件：`<path>`

如果有任何"必须人工确认"的环节，**在这里明说**，因为 R5 规则不允许自动化无法跨越的步骤——要么删掉，要么 override 时说明原因。

## 四、错误处理 / 重试

- 哪些错误可以原地重试？重试几次？
- 哪些错误必须停下来报警？
- 跑一半挂了，再跑一次会不会有副作用（重复写库、文件覆盖、token 浪费）？

## 五、预期耗时

正常情况下这个流程跑多久？超过多久要认为它卡死了？

（如果有 outputs.md，把产出清单挪过去；如果没有，在这里列出。）
