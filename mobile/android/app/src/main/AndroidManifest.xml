<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <!-- Android 14 requires a typed foreground service permission. dataSync
         matches best for a persistent user-initiated tunnel. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <!-- Android 13+ requires runtime permission for notifications. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Keep tunnel alive across screen-off. WAKE_LOCK is the polite way;
         we don't want to be a full-power hog. -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!-- WAKE_LOCK keeps the CPU on but does NOT exempt us from Doze network
         deferral / OEM battery-killers. This lets us prompt the user to grant
         a battery-optimization exemption so the tunnel survives screen-off. -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <application
        android:name=".App"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.CSMTunnel"
        android:usesCleartextTraffic="true"
        tools:targetApi="34">

        <!-- Cleartext for localhost:8000 loaded in WebView (the whole point). -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".DashboardActivity"
            android:exported="false"
            android:label="@string/app_name"
            android:parentActivityName=".MainActivity" />

        <activity
            android:name=".WebViewActivity"
            android:exported="false"
            android:label="@string/webview_title"
            android:parentActivityName=".DashboardActivity"
            android:configChanges="orientation|screenSize|keyboardHidden" />

        <service
            android:name=".SshTunnelService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

    </application>
</manifest>
